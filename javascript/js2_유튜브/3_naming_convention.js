/**
 * 네이밍 컨벤션
 * 
 * 변수 이름 지을때
 * 1) 일반적으로 영어(라틴문자)를 사용하며 문자와 숫자를 모두 사용할 수 있다.
 * 2) 특수기호는 언더스코어(_)와 달러($)를 사용할 수 있다.
 * 3) 숫자로 이름을 시작할 수 없다.
 *      1Name , 2Helo - nono
 * 4) 키워드는 변수명으로 사용할 수 없다 var let = 'var';
 */
let codefactory = '코드팩토리';

var $ive = '아이브';
const _girl = '소녀';

console.log(codefactory);
console.log($ive);
console.log(_girl);

// let 1name = 'nono';
// let const = 'const nono';

/**
 * Naimg convention 2
 * 1) 카멜케이스 : codeFactory -> 대부분의 언어에서 사용
 * 2) snake_case : math_score -> 파이썬같은 언어에서 많이 사용
 */
const myHome = '경기도';
console.log(myHome);